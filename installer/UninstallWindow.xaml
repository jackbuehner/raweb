<Window x:Class="RAWebInstaller.UninstallWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="RAWeb Installer"
        Width="560"
        ResizeMode="NoResize"
        SizeToContent="Height"
        ThemeMode="System">

  <Window.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/PresentationFramework.Fluent;component/Themes/Fluent.xaml"/>
      </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
  </Window.Resources>


  <Grid Background="{DynamicResource LayerFillColorAltBrush}">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <StackPanel Margin="24,24,24,0"
                Grid.Row="0">
      <!-- Title -->
      <TextBlock x:Name="MessageTitle"
                 TextWrapping="Wrap"
                 Style="{StaticResource SubtitleTextBlockStyle}"
                 Margin="0,0,0,24"/>
      <TextBlock x:Name="MessageText"
                 TextWrapping="Wrap"
                 Style="{StaticResource BodyTextBlockStyle}"
                 Margin="0,0,0,16">
        Installed files will be removed, and the IIS website and 
        application pool will be deleted. IIS features will not be uninstalled.
      </TextBlock>
      <CheckBox x:Name="RemoveAppDataCheckBox"
                Content="Also remove app data"
                IsChecked="False"
                FontSize="14"
                Margin="0,0,0,0"/>
    </StackPanel>

    <!-- Status Text -->
    <StackPanel x:Name="StatusPanel"
                Grid.Row="1"
                Grid.ColumnSpan="2"
                Margin="0"
                Height="24">
      <TextBlock x:Name="LogBox"
                 Text=""
                 VerticalAlignment="Center"
                 HorizontalAlignment="Left"
                 Height="20"
                 Style="{StaticResource BodyTextBlockStyle}">
      </TextBlock>
      <ProgressBar x:Name="ProgressBar"
                   Height="6"
                   IsIndeterminate="True"
                   Visibility="Collapsed">
      </ProgressBar>
    </StackPanel>

    <Border
      Grid.Row="2"
      Padding="24,24,24,24"
      Background="{DynamicResource SolidBackgroundFillColorBaseBrush}"
      BorderThickness="0">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*"/>
          <ColumnDefinition Width="16"/>
          <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Button x:Name="UninstallButton"
                Click="Uninstall_Click"
                Content="Uninstall"
                Grid.Column="0"
                HorizontalAlignment="Stretch"
                Height="32"
                FontSize="14"/>
        <Button x:Name="CancelButton"
                Click="Cancel_Click"
                Content="Cancel"
                Grid.Column="2"
                HorizontalAlignment="Stretch"
                Height="32"
                FontSize="14"/>
      </Grid>
    </Border>
  </Grid>

</Window>
